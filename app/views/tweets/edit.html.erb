<div class="contents row">
  <div class="container">
    <%= form_with(model: @tweet) do |form| %>
      <h3>
        編集する
      </h3>
      <%= render partial: "form", locals: { form: form } %>
    <% end %>
  </div>
</div>
